/* Un componente es una función. Los nombres de los componentes deben tener la inicial mayúscula */
function Header() {

    return(
        <h1>Retornando desde Header</h1>
    )
}

export default Header // Con esto exportamos el componente